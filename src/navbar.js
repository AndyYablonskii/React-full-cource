const Navbar = () => {
    return (  
    <nav className="Navbar">
        <h1>The to do list</h1>
        <div className="links">
            <a href="/">Home</a>
            <a href="/create">Create a new task</a>
        </div>
    </nav> 
    );
}

export default Navbar;